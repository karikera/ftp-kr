{
	"allOf": [{"$ref": "server.schema.json"}],
	"type": "object",
    "properties": {
		"closure": {
			"deprecationMessage": "Closure Compiler feature is splitted. If you want to use, Please download Closure Compiler extension."
		},
		"altServer": {
			"type": "array",
			"description": "alternate servers. It has similar properties with root manifest",
			"items": {
				"type": "object",
				"allOf": [{"$ref": "server.schema.json"}],
				"properties": {
					"name":{
						"type":"string",
						"description": "Display name for alternate server"
					}
				}
			}
		},
		"localBasePath":{
			"type": "string",
			"desciption": "Set local directory to sync (default: workspace root)"
		},
        "autoUpload": {
            "type": "boolean",
            "description": "Upload file when save"
        },
        "autoDelete": {
            "type": "boolean",
            "description": "Delete FTP file when delete in workspace"
        },
		"autoDownload": {
			"type": "boolean",
			"description": "It will check modification of every opening and download if it modified"
		},
		"autoDownloadAlways": {
			"type": "number",
			"description": "Check server modification at set time intervals and download it"
		},
		"logLevel": {
			"enum": [
				"VERBOSE",
				"NORMAL"
			],
            "description": "Log level setting for debug"
		},
		"ignore": {
			"type": "array",
			"description": "Ignore file or directory list. Is NOT glob pattern",
			"items": {
				"type": "string",
				"description": "Ignore file or directory"
			}
		}
    }
}