{
    "type": "object",
    "properties": {
        "name":{
            "type": "string",
            "description": "Project name, Only for console output!"
        },
        "src":{
            "anyOf": [{"type": "string"}, {"type": "array" }],
            "description": "Javascript sources, redirect to --js arguments, Can use glob pattern."
        },
        "output":{
            "type": "string",
            "description": "Output file path!"
        },
        "includeReference": {
            "type": "boolean",
            "description": "Parse <reference> XML tag and include it automatically!"
        },
        "closure": {"$ref": "closure.schema.json"}
    }
}